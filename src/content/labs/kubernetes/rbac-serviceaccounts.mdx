---
title: "RBAC e ServiceAccounts com privilégio mínimo"
description: "Construa uma Role limitada, diagnostique autorização negada e conceda somente as permissões necessárias."
category: "Kubernetes"
level: "Intermediário"
duration: 40
updatedAt: 2026-08-26
prerequisites: ["Docker, kind, kubectl, Git e Make", "Namespaces e recursos da API"]
objectives: ["Usar kubectl auth can-i", "Relacionar Role e RoleBinding", "Aplicar privilégio mínimo"]
tools: ["kind", "kubectl", "RBAC"]
certifications: ["CKA", "CKS"]
certificationDomain: "Cluster configuration e acesso"
testedWith: ["Kubernetes 1.34"]
repositoryPath: "labs/kubernetes/rbac-serviceaccounts"
draft: false
featured: true
---
## Preparar e testar permissões
```bash
git clone https://github.com/ndawpa/platformengineering-website.git
cd platformengineering-website/labs/kubernetes/rbac-serviceaccounts
make setup
kubectl auth can-i get pods -n pe-rbac --as=system:serviceaccount:pe-rbac:viewer
kubectl auth can-i list pods -n pe-rbac --as=system:serviceaccount:pe-rbac:viewer
```
A Role permite `get`, mas o objetivo exige também `list`. Corrija sem conceder `delete`:
```bash
kubectl patch role pod-viewer -n pe-rbac --type=json -p='[
  {"op":"add","path":"/rules/0/verbs/-","value":"list"}
]'
make verify
```
Inspecione `RoleBinding` e confirme que o token não é montado automaticamente no ServiceAccount.
## Limpar
```bash
make cleanup
```
