---
import { SITE } from '../config/site';
const enabled = SITE.analytics.enabled && SITE.analytics.cloudflareToken;
---
{enabled && <script is:inline defer src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon={JSON.stringify({ token: SITE.analytics.cloudflareToken })}></script>}
